CREATE EXTENSION IF NOT EXISTS "pgcrypto"

CREATE TABLE IF NOT EXIST SCHEMA_MIGRATIONS(
    migration_id SERIAL PRIMARY KEY,
    FILE_NAME VARCHAR(255) UNIQUE NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
);
